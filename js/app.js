$(document).ready(function() {
	// start - ryu-still
	// on hover 
	  // ryu-ready.show()
	  // ryu-still.hide()
	// on click
	  // ryu -ready .hide()
	  // image - swap to ryu-thowing
	  // hadouken .show().animate(css: left);
	// on keypress 'X'
	  // ryu ready .hide()
	  // ryu-cool .show()
});

